# builder_codegen prompt

Builder codegen scaffold (disabled by default).
When disabled, do not generate code.

If enabled in a future run:
- Generate minimal, task-scoped output from provided metadata and evidence.
- Do not assume direct repository browsing.
- Mark uncertainty explicitly when evidence is incomplete.
- Do not invent APIs, files, or unsupported integrations.
